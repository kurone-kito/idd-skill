#!/bin/sh
# -*- mode: sh -*-
# vim: set ft=sh :

# Chain to the IDD worktree guard, which refuses a push from the primary
# worktree while HEAD is on an `issue/*` or `roadmap-audit/*` branch.
exec "$(git rev-parse --show-toplevel)/.githooks/pre-push" "$@"
