## Background

An adopter session flagged this problem while running the loop, but the
drafted body never gained the `{marker-prefix}-upstream-candidate` marker
even though the `status:upstream-candidate` label was applied.

## Proposed change

Fix the local problem this issue describes.

## Acceptance criteria

- [ ] the local fix is verified

---

_Autopilot suitability: 4 / 5 -- higher is more autopilot-suitable;
below the configured floor is human-oriented._

<!-- example-adopter-autopilot-suitability: 4 -->
